{
  "name": "ajax_table_jquery",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "fake-json": "json-server ./assets/db.json --watch --port 3001"
  },
  "devDependencies": {
    "vite": "^3.1.0"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
